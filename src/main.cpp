#include "CRrenderer.h"
using namespace glm;

int main() {
    // RenderableObject* obj = new RenderableObject("resource/model.obj");
    // Scene* s = new Scene();
    // s->addObject(obj);
    // RayTracingShader * r = new RayTracingShader(s);
    // r->render();
}